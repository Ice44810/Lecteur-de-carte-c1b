# Runtime dependencies - tachy-linux
#
# Installation (Ubuntu / Debian):
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# pyscard needs system packages first (see docs/INSTALL_LINUX.md):
#   sudo apt install pcscd pcsc-tools libpcsclite-dev python3-dev build-essential swig

PySide6>=6.6
SQLAlchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
openpyxl>=3.1
reportlab>=4.0

# PC/SC smart card access (optional: the application starts and runs without it)
pyscard>=2.0
